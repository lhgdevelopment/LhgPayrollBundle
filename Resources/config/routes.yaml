controllers:
    resource: '../../Controller/'
    exclude: '../../Controller/API'
    type: annotation
    prefix: /{_locale}
    requirements:
        _locale: '%app_locales%'
    defaults:
        _locale: '%locale%'
